# Esta aplicação é 100% client-side e NÃO usa credenciais bancárias.
# Nenhuma chave secreta deve ser colocada aqui ou versionada.
#
# Único ajuste de build (opcional): caminho base para o GitHub Pages.
# Em repositório https://github.com/<usuario>/planilha o valor é /planilha/
VITE_BASE_PATH=/planilha/
